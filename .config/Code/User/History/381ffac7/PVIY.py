import setuptools as setup 

setup(
    name = 'coordinates-analyzer',
    version = '0.0.1',
    description = 'A Python CLI program that can compare, mathematically 2 different molecular structure (.xyz file), based on their atomic coordinates, th'
)